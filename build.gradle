plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.3.61'
}
repositories {
    mavenCentral()
}
kotlin {
    linuxX64("linux") {
        binaries {
            executable {
                entryPoint = 'sample.main'
            }
            staticLib {
                baseName = 'sample'
            }
            sharedLib {
                baseName = 'sample'
            }
        }
    }
    sourceSets {
        linuxMain {
        }
        linuxTest {
        }
    }
}
